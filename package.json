{
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "prisma:generate": "npm run postinstall --prefix frontend",
    "prisma:push": "npx prisma db push --schema=database/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^7.4.1",
    "@prisma/adapter-pg": "^6.3.1",
    "pg": "^8.13.1",
    "bcryptjs": "^2.4.3",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "prisma": "^7.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "prisma": {
    "seed": "node database/prisma/seed.js"
  }
}